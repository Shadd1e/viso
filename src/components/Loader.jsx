export default function Loader() {
  // The site has outgrown the old door-opening intro animation.
  // Keep the component in place so existing imports do not need to change,
  // but render nothing so the homepage opens directly.
  return null
}
