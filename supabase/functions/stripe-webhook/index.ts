const corsHeaders={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"content-type","Access-Control-Allow-Methods":"POST, OPTIONS"}
function json(data:unknown,status=200){return new Response(JSON.stringify(data),{status,headers:{...corsHeaders,"Content-Type":"application/json"}})}
function hex(bytes:ArrayBuffer){return [...new Uint8Array(bytes)].map(b=>b.toString(16).padStart(2,'0')).join('')}
async function signatureValid(payload:string,header:string,secret:string){const parts=header.split(',');const t=parts.find(x=>x.startsWith('t='))?.slice(2);const sigs=parts.filter(x=>x.startsWith('v1=')).map(x=>x.slice(3));if(!t||!sigs.length)return false;if(Math.abs(Date.now()/1000-Number(t))>300)return false;const key=await crypto.subtle.importKey('raw',new TextEncoder().encode(secret),{name:'HMAC',hash:'SHA-256'},false,['sign']);const digest=hex(await crypto.subtle.sign('HMAC',key,new TextEncoder().encode(`${t}.${payload}`)));return sigs.some(s=>s===digest)}
async function sb(path:string,init:RequestInit={}){const url=Deno.env.get('SUPABASE_URL')!,key=Deno.env.get('VISO_SUPABASE_SERVICE_ROLE_KEY')!;const r=await fetch(`${url}/rest/v1/${path}`,{...init,headers:{apikey:key,Authorization:`Bearer ${key}`,'Content-Type':'application/json',...(init.headers||{})}});if(!r.ok)throw new Error(await r.text());return r.status===204?null:r.json()}
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:corsHeaders});if(req.method!=='POST')return json({error:'Method not allowed'},405);try{const payload=await req.text();const secret=Deno.env.get('STRIPE_WEBHOOK_SECRET');const sig=req.headers.get('stripe-signature');if(!secret||!sig)return json({error:'Webhook secret is not configured'},500);if(!(await signatureValid(payload,sig,secret)))return json({error:'Invalid Stripe signature'},400);const event=JSON.parse(payload),type=event.type,obj=event.data?.object;if(type==='checkout.session.completed'||type==='checkout.session.async_payment_succeeded'){const bookingId=obj?.metadata?.booking_id;if(bookingId){await sb(`bookings?id=eq.${encodeURIComponent(bookingId)}`,{method:'PATCH',body:JSON.stringify({payment_status:'paid',status:'confirmed',stripe_checkout_session_id:obj.id,stripe_payment_intent_id:obj.payment_intent||null,updated_at:new Date().toISOString()})});await sb('payments',{method:'POST',headers:{Prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify({booking_id:bookingId,stripe_checkout_session_id:obj.id,stripe_payment_intent_id:obj.payment_intent||null,amount:Number(obj.amount_total||0)/100,currency:String(obj.currency||'usd'),status:'paid',updated_at:new Date().toISOString()})})}}else if(type==='checkout.session.async_payment_failed'||type==='payment_intent.payment_failed'){const bookingId=obj?.metadata?.booking_id;if(bookingId)await sb(`bookings?id=eq.${encodeURIComponent(bookingId)}`,{method:'PATCH',body:JSON.stringify({payment_status:'failed',updated_at:new Date().toISOString()})})}else if(type==='charge.refunded'){const pi=obj?.payment_intent;if(pi)await sb(`payments?stripe_payment_intent_id=eq.${encodeURIComponent(pi)}`,{method:'PATCH',body:JSON.stringify({status:'refunded',updated_at:new Date().toISOString()})})}return json({received:true})}catch(e){console.error(e);return json({error:'Webhook processing failed'},500)}})
